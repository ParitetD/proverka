PORT=4000
CORS_ORIGIN=http://localhost:5173

# Секрет для JWT (замените в проде)
JWT_SECRET=change-me-please

# Путь к файлу базы (JSON) (относительно папки backend или абсолютный)
DATABASE_URL=data/db.json

